Create a Timer Web Application. The app should have a timer that visually counts in 2-second increments, but the actual time behind it should still be accurate and continue updating every second.

The app theme should use orange and yellow colors.

